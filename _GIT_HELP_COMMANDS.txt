git init | Initialized empty Git repository in E:/OneDrive/z/zooid/GIT/zenva_git/.git/
git --config | show available commands for configuration
git --config --system | system = server
git --config --global | global = current user
git --config --local | local = user+project
git status | gives modified/staged files
git add * | staging all files (stage = prepared for commit)
git add *.txt | staging all .txt files
git config --global --get user.name | displays the name of the current user
git config --global user.name "some name" | change the name of the current user quotations are used if there's a space
git config --global --get user.email | displays the email of the current user
git config --global user.email some@email.com | sets the email
git log | displays info about all previous commits
git restore --staged * | un-stage all staged files (files are still modified but not staged nor commited)
git restore * | modified (and not staged) files are restored to their previous version (all modification are undone)
git reset --hard {hash code of the commit} | restores previous commit completely undoing every change done after
git branch | list all branches
git checkout -b development | create new branch with (-b) with name "development" (branch = essentially a copy of our project) (checkout = create+switch to that branch)
git checkout master | switching to the master branch
git merge development | first we need to switch to master before merging
git branch -d development | deleting the "development" branch
git push | uploading the local commit to the online repository
git pull | downloading all changes from the online to the local repository
git push origin development | ?